# 邮件缓存 vs 数据库存储对比图

## 简单对比 (Simple Comparison)

```
📧 邮件缓存存储在哪里？
========================

❌ 不在数据库中！
   NOT in Database!

✅ 在 Flask Session 文件中！
   In Flask Session Files!


存储位置对比:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 flask_session/               📁 db/
   ├── session_file_1              └── mail.sqlite
   ├── session_file_2                  ├── cards 表
   └── session_file_3                  ├── mail_accounts 表
   
   邮件缓存存储在这里           卡密和邮箱存储在这里
   (Mail cache here)           (Cards & emails here)
   
   ⏱️  临时 (5分钟)               ♾️  永久
   🔄 自动清理                   📝 手动管理
```

## 详细架构图 (Detailed Architecture)

```
┌────────────────────────────────────────────────────────────────┐
│                        邮件系统完整架构                          │
│                   (Complete Mail System Architecture)          │
└────────────────────────────────────────────────────────────────┘

用户请求流程:
═══════════

1️⃣  用户点击"获取邮件"
    │
    ▼
┌─────────────────────────┐
│  调用 /api/preview_mail │
└───────────┬─────────────┘
            │
            ▼
    ┌───────────────┐
    │  邮件服务器   │  ← 获取邮件 (2-3秒)
    │ (IMAP/POP3)  │
    └───────┬───────┘
            │
            ▼
    ┌─────────────────────────────────────┐
    │  缓存到 Flask Session (文件系统)    │
    │  flask_session/xxx...xxx            │
    │  ┌───────────────────────────────┐  │
    │  │ mail_cache_CARD123_user@...  │  │
    │  │ {                             │  │
    │  │   mail_data: {...},          │  │
    │  │   timestamp: 1234567890      │  │
    │  │ }                             │  │
    │  └───────────────────────────────┘  │
    └─────────────────────────────────────┘
            │
            ▼
    返回预览信息给用户
    (只显示标题、发件人、时间)


2️⃣  用户点击"查看完整邮件"
    │
    ▼
┌─────────────────────────┐
│  调用 /api/view_mail    │
└───────────┬─────────────┘
            │
            ▼
    ┌─────────────────────────────┐
    │  检查 Flask Session 缓存    │
    └─────────┬───────────────────┘
              │
         ┌────┴────┐
         │         │
    有效缓存      缓存过期
    (< 5分钟)     (> 5分钟)
         │         │
         ▼         ▼
    使用缓存    重新获取
    (0.1秒)     (2-3秒)
         │         │
         └────┬────┘
              │
              ▼
      返回完整邮件内容
      (包含正文、附件等)


存储层级架构:
═══════════

┌─────────────────────────────────────────────────────────┐
│                      服务器文件系统                      │
│                    (Server Filesystem)                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  📁 项目根目录/                                         │
│  │                                                      │
│  ├─ 📁 flask_session/  ← 邮件缓存存储目录              │
│  │  │                                                   │
│  │  ├─ 📄 2029240f... (User A 的 session)              │
│  │  │     └─ mail_cache_CARD1_user1@example.com       │
│  │  │        └─ 邮件内容 + 时间戳                      │
│  │  │                                                   │
│  │  ├─ 📄 3f2d8e9c... (User B 的 session)              │
│  │  │     └─ mail_cache_CARD2_user2@example.com       │
│  │  │        └─ 邮件内容 + 时间戳                      │
│  │  │                                                   │
│  │  └─ 📄 ... (更多 session 文件)                      │
│  │                                                      │
│  ├─ 📁 db/  ← 数据库目录                               │
│  │  │                                                   │
│  │  └─ 📄 mail.sqlite  ← SQLite 数据库文件            │
│  │     │                                                │
│  │     ├─ 📊 cards 表                                  │
│  │     │  ├─ id, card_key, usage_limit                │
│  │     │  ├─ used_count, status                       │
│  │     │  └─ last_mail_subject, last_mail_date ⭐     │
│  │     │                                               │
│  │     ├─ 📊 mail_accounts 表                         │
│  │     │  └─ 邮箱账号配置                             │
│  │     │                                               │
│  │     └─ 📊 card_logs 表                             │
│  │        └─ 使用日志                                 │
│  │                                                      │
│  └─ 📄 app.py  ← 应用程序                             │
│                                                         │
└─────────────────────────────────────────────────────────┘


数据对比表:
═════════

┌──────────────┬─────────────────┬─────────────────┐
│   数据类型   │  存储位置       │    持久性       │
├──────────────┼─────────────────┼─────────────────┤
│ 邮件缓存     │ flask_session/  │ 临时(5分钟)     │
│ 卡密信息     │ db/mail.sqlite  │ 永久            │
│ 邮箱账号     │ db/mail.sqlite  │ 永久            │
│ 使用日志     │ db/mail.sqlite  │ 永久            │
│ 最后邮件记录 │ db/mail.sqlite  │ 永久 ⭐         │
└──────────────┴─────────────────┴─────────────────┘

⭐ 注意: 虽然邮件缓存是临时的，但最后邮件的标题、
   日期、message_id 会保存到数据库中，用于判断
   是否是同一封邮件，避免重复扣费。


为什么这样设计？
═══════════════

✅ 邮件缓存用 Session (文件):
   • 临时数据，不需要永久保存
   • 自动过期清理，无需维护
   • 读写速度快
   • 不占用数据库空间

✅ 卡密信息用 Database:
   • 永久数据，需要长期保存
   • 需要查询和统计
   • 需要跨会话访问
   • 需要事务保证


实际文件内容示例:
═══════════════

📁 flask_session/2029240f6d1128be89ddc32729463129
内容: (加密的二进制数据，包含):
{
  'mail_cache_CARD123_user@example.com': {
    'mail_data': {
      'success': True,
      'mail': {
        'subject': '重要通知',
        'from': 'admin@example.com',
        'body': '邮件正文内容...',
        'attachments': [...]
      }
    },
    'timestamp': 1234567890.123
  }
}

📄 db/mail.sqlite (cards 表)
┌────┬──────────┬──────────┬───────────────┬─────────────┐
│ id │ card_key │ used_cnt │ last_mail_sub │ last_mail_d │
├────┼──────────┼──────────┼───────────────┼─────────────┤
│ 1  │ CARD123  │ 5        │ 重要通知      │ 2024-02-07  │
└────┴──────────┴──────────┴───────────────┴─────────────┘


时间线对比:
═════════

Session 缓存生命周期:
━━━━━━━━━━━━━━━━━
T=0    │ 创建缓存 (preview_mail)
       │
T=30s  │ 使用缓存 (view_mail) ✅
       │
T=2min │ 使用缓存 (view_mail) ✅
       │
T=5min │ 缓存过期 ⏰
       │
T=6min │ 重新获取 (view_mail) 🔄
       │
T=?    │ Flask 自动清理过期 session 🗑️


数据库数据生命周期:
━━━━━━━━━━━━━━━━
T=0    │ 创建卡密
       │
T=任何  │ 随时可查询 ✅
       │
T=删除  │ 手动删除 (或过期移入回收站)
       │
♾️      │ 永久保存


总结图示:
════════

            邮件系统存储架构
            ═══════════════

    临时存储层 (Temporary Storage)
    ━━━━━━━━━━━━━━━━━━━━━━━━━
    📁 flask_session/
       └─ 邮件内容缓存 (5分钟)
          └─ 快速访问 (0.1秒)
          └─ 自动清理


    永久存储层 (Permanent Storage)
    ━━━━━━━━━━━━━━━━━━━━━━━━━
    📁 db/mail.sqlite
       ├─ 卡密信息
       ├─ 邮箱账号
       ├─ 使用日志
       └─ 最后邮件元数据 ⭐
```

**关键要点**: 
- ✅ 邮件缓存在 `flask_session/` 目录（文件系统）
- ❌ 邮件缓存不在数据库中
- ⭐ 但最后邮件的元数据（标题、日期）会保存到数据库
